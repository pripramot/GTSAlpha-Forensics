# GTSAlpha-Forensics — ตัวแปรแวดล้อม (เชื่อม Supabase ก่อน; บัญชีอื่นจะแจ้งทีหลัง)
# Copy เป็น .env แล้วกรอกค่า

# Supabase (โครงการ: GTSAlpha-Forensics)
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your_anon_key_here
SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# Docker (Local PostgreSQL) — ใช้เมื่อรัน docker-compose
# DATABASE_URL=postgresql://super_admin:secret_unicorn_password@localhost:5432/unicorn_gts
